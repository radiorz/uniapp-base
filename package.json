{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "voerka-cloud-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "del": "del-cli --help",
    "server-template:deploy": "del-cli deploy/server-template --force && pnpm --filter server-template deploy deploy/server-template && cd deploy/server-template && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.1.0"
  }
}
